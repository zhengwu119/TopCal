name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify Swift toolchain
        run: swift --version

      - name: Build app
        run: ./build.sh

      - name: Verify app bundle
        run: |
          test -x build/TopCal.app/Contents/MacOS/TopCal
          test -d build/TopCal.app/Contents/Resources/en.lproj
          codesign --verify build/TopCal.app
          echo "Build and signature verification passed"
